# 📊 ML 모델 최적화 심층 분석 보고서
**작성일**: 2026-01-09  
**대상**: LightGBM Classification Model Optimization  
**비교군**: `20260108` (1000 Trials) vs `20260109` (2000 Trials)

---

## 1. 📈 성과 요약 (Executive Summary)
하루 만에 모델의 예측 성능(Average Precision)이 **약 16.8% 향상**되는 괄목할 만한 성과가 있었습니다.

| 구분 | 2026-01-08 (어제) | 2026-01-09 (오늘) | 증감 (Delta) |
| :--- | :---: | :---: | :---: |
| **Best AP Score** | `0.2294` | **`0.2679`** | **▲ +0.0385 (+16.8%)** |
| **Trials** | 1,000회 | 2,000회 | +1,000회 (Deep Search) |
| **Model Trend** | Complex & Slow | **Simple & Agile** | 구조적 변화 |

> **💡 핵심 발견**:  
> 어제의 모델은 "너무 신중해서(Low LR) 복잡하게 꼬인(Large Tree)" 상태였으나,  
> 오늘의 모델은 **"단순한 구조(Small Tree)로 과감하게 학습(High LR)"**하는 방향으로 진화하여 일반화 성능이 대폭 개선되었습니다.

---

## 2. 🔍 파라미터 수렴 및 상관관계 분석 (Parameter Convergence)

데이터 분석 결과, 점수가 높아질수록 파라미터들이 특정 방향으로 강하게 수렴하는 현상이 발견되었습니다.

### 2.1 주요 파라미터 변화
| 파라미터 (Parameter) | 의미 | 어제 (Best) | **오늘 (Best)** | 변화 해석 |
| :--- | :--- | :--- | :--- | :--- |
| **`learning_rate`** | 학습 속도 | `0.00448` (매우 느림) | **`0.01465`** (빠름) | **약 3.3배 증가**. 과감한 업데이트로 Local Minima 탈출. |
| **`num_leaves`** | 트리 복잡도 | `249` (매우 복잡) | **`60`** (단순) | **1/4로 감소**. 과적합(Overfitting) 방지 및 추론 속도 향상. |
| **`colsample_bytree`** | 피처 샘플링 | `0.515` (50%만 봄) | **`0.937`** (94% 봄) | 거의 모든 피처를 종합적으로 고려함. |
| **`scale_pos_weight`** | 불균형 가중치 | `5.31` | **`8.33`** | 희소한 '상승(1)' 케이스를 더 중요하게 학습함. |

### 2.2 상관관계 (Correlation) 분석
상위권 모델들에서 나타나는 공통적인 특징(Correlation with Score)입니다.

1.  **`colsample_bytree` (양의 상관관계 +0.35)**:
    - **해석**: 트리가 분기할 때 더 많은 피처를 후보로 고려할수록 성능이 좋아집니다. 이는 현재 데이터셋의 피처들이 고르게 유의미함을 시사합니다.
2.  **`num_leaves` (음의 상관관계 -0.21)**:
    - **해석**: 리프 노드 수가 적을수록 점수가 높습니다. 즉, 모델이 너무 미세한 패턴까지 외우려 하기보다, **굵직한 패턴 위주로 학습할 때 실전 성능이 더 뛰어남**을 의미합니다.

---

## 3. 🚀 성능 급등의 원인 분석 (Root Cause Analysis)

어제보다 성능이 급격히 좋아진 이유는 크게 두 가지로 분석됩니다.

### 3.1 양적 개선: Deep Search (2000 Trials)
- **현상**: 어제 1,000회 시도 시점에서는 `learning_rate`가 낮은 구간(0.004)에 갇혀(Local Optima) 더 이상 성능이 오르지 않았습니다.
- **개선**: 시도 횟수를 2,000회로 늘리면서 Optuna가 "학습률을 높이고 트리를 단순화하는" 새로운 조합을 탐험할 기회를 얻었고, 이 구간이 훨씬 더 높은 잠재력(Global Optima)을 가짐을 발견했습니다.

### 3.2 질적 개선: Data Integrity Fixes
- **배경**: 어제(1/8) 오후, **데이터 경로 불일치 수정** 및 **피처 생성 기간 자동화** 개발이 진행되었습니다. (참고: `develop.md`)
- **영향**: 어제 최적화 시점에는 일부 최신 데이터나 특정 구간 데이터가 누락되었을 가능성이 컸으나, 오늘의 최적화에는 **수정된 파이프라인을 통해 온전한 데이터셋(Full Integrity)**이 공급되었습니다. 데이터의 품질 향상이 모델 성능 향상으로 직결되었습니다.

---

## 4. ✅ 결론 및 제안 (Conclusion & Recommendation)

현재 도출된 파라미터는 **"단순함이 복잡함을 이긴다(Simple is Best)"**는 머신러닝의 대원칙을 보여주는 모범적인 사례입니다.

1.  **파라미터 채택**: `20260109` 로그의 최적 파라미터(AP 0.2679)를 즉시 운영 설정(`settings.yaml`)에 반영하십시오. (이미 자동 저장됨)
2.  **모델 교체**: 과적합 위험이 낮고 예측력이 높은 새 모델로 `lgbm_model.joblib`을 재생성(학습)하십시오.
3.  **지속 모니터링**: 향후에도 `num_leaves`가 낮고 `learning_rate`가 적당히 높은 이 경향성이 유지되는지 주기적인 관찰이 필요합니다.

---
*Report generated by SpikeHunter AI Agent*
