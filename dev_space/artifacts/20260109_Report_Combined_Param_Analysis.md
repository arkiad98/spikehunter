# 20260109 통합 파라미터 분석 보고서 (4000건)

## 1. 개요
- **분석 대상**:
    1. `logs/20260109_131732_pipeline.json` (2000회)
    2. `logs/20260109_134614_pipeline.json` (2000회)
- **총 시도 횟수**: 4000회
- **검증 범위**: 상위 10% (Top 400) 및 상위 10 (Top 10)

## 2. 분석 결과 요약

### 2.1 파라미터 수렴도 (Top 10 기준)

| 파라미터 | 평균값 | 표준편차 | 수렴 상태 | 비고 |
| :--- | :--- | :--- | :--- | :--- |
| **target_r** | **0.0625** | 0.0006 | **수렴 (Converged)** | 매우 강력한 수렴 (약 6.25%) |
| **stop_r** | **-0.0315** | 0.0012 | **수렴 (Converged)** | 강력한 수렴 (약 -3.15%) |
| **min_ml_score** | **0.5192** | 0.0493 | **안정 (Stable)** | 0.52 부근에서 안정적 (CV < 0.1) |
| **min_mfi** | 48.10 | 7.46 | **불안정 (Unstable)** | 40~60 사이 넓은 분포 |
| **vbo_k** | 0.54 | 0.13 | **불안정 (Unstable)** | 0.33 ~ 0.66 넓은 분포 |
| **max_market_vol** | 0.24 | 0.06 | **불안정 (Unstable)** | 0.17 ~ 0.39 넓은 분포 |

### 2.2 상위 10% (Top 400) 경향성
- **target_r**와 **stop_r**는 상위 400개 시도에서도 매우 낮은 편차(CV 0.03~0.08)를 보이며 일관된 수렴 패턴을 유지합니다. 이는 이 두 파라미터가 전략 성능의 핵심임을 시사합니다.
- **min_ml_score**는 상위권(Top 10)에서는 0.52로 높게 형성되나, Top 10% 전체 평균은 0.47로 다소 낮아집니다. 즉, 고득점을 위해서는 0.5 이상의 높은 ML Score 임계값이 유리함을 알 수 있습니다.
- **max_market_vol**과 **vbo_k**는 전체적으로 편차가 크며(CV > 0.2), 점수 변동성의 주요 원인으로 추정됩니다.

## 3. 결론 및 제언

1.  **핵심 파라미터 확정**: `target_r`(0.0625)와 `stop_r`(-0.0315)는 최적값으로 수렴했다고 판단됩니다. 향후 최적화 시 이 값들을 고정하거나 매우 좁은 범위로 제한하여 탐색 효율을 높일 수 있습니다.
2.  **점수 비일관성의 원인**: `vbo_k`(변동성 돌파 계수)와 `max_market_vol`(시장 변동성 제한)이 수렴하지 않고 넓게 퍼져 있어, 이들이 시도 별 점수 차이를 만드는 주요 변수(Noise Factor)로 작용하고 있습니다.
3.  **ML Score 상향**: 상위 10개 시도의 평균 `min_ml_score`가 0.52로 상당히 높습니다. 안정적인 고수익을 위해 임계값을 0.50 이상으로 상향 조정하는 것을 고려해볼 만합니다.
4.  **권장 파라미터 세팅**:
    - `target_r`: 0.0625
    - `stop_r`: -0.0315
    - `min_ml_score`: 0.52 (또는 0.50 이상)
    - `min_mfi`: 48 (다만 민감도 낮음)
